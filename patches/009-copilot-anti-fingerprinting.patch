# PATCH 009: Copilot anti-fingerprinting and conversation-aware initiator control
#
# Restores the fork-specific Copilot fingerprinting behavior on top of the
# current upstream executor:
# - deterministic per-account header fingerprints and VS Code IDs
# - warm-conversation tracking with persistent cache across restarts
# - active execution sessions stay agent-initiated once warm
# - restored warm roots ramp real initiator probability from 0% over time
# - targeted regression tests for the warm/fingerprint helpers
diff --git a/internal/runtime/executor/github_copilot_executor.go b/internal/runtime/executor/github_copilot_executor.go
--- a/internal/runtime/executor/github_copilot_executor.go
+++ b/internal/runtime/executor/github_copilot_executor.go
@@ -4,14 +4,22 @@
 	"bufio"
 	"bytes"
 	"context"
+	"crypto/sha256"
+	"encoding/binary"
+	"encoding/hex"
+	"encoding/json"
+	"errors"
 	"fmt"
 	"io"
 	"net/http"
+	"os"
+	"path/filepath"
 	"slices"
 	"strings"
 	"sync"
 	"time"
 
+	"github.com/gin-gonic/gin"
 	"github.com/google/uuid"
 	copilotauth "github.com/router-for-me/CLIProxyAPI/v6/internal/auth/copilot"
 	"github.com/router-for-me/CLIProxyAPI/v6/internal/config"
@@ -45,13 +53,43 @@
 	copilotOpenAIIntent  = "conversation-edits"
 	copilotGitHubAPIVer  = "2025-10-01"
 	copilotThinkingBeta  = "interleaved-thinking-2025-05-14,context-management-2025-06-27"
+
+	copilotSessionRotation                 = 4 * time.Hour
+	copilotWarmConversationTTL             = 6 * time.Hour
+	copilotWarmConversationRealStep        = 0.005
+	copilotWarmConversationRealStepWindow  = 2 * time.Hour
+	copilotWarmConversationReservationTTL  = 45 * time.Second
+	copilotWarmConversationCleanupInterval = 15 * time.Minute
+	copilotWarmConversationStateFilename   = "copilot-warm-conversations.json"
+	copilotWarmConversationSessionHeader   = "X-Session-Affinity"
+)
+
+var (
+	copilotEditorVersions = []string{
+		"vscode/1.109.2",
+		"vscode/1.109.3",
+	}
+	copilotPluginVersions = []string{
+		"copilot-chat/0.37.2026013101",
+		"copilot-chat/0.37.2026020301",
+	}
+	copilotUserAgents = []string{
+		"GithubCopilot/1.0",
+		"GitHubCopilotChat/0.37.0",
+	}
 )
 
 // GitHubCopilotExecutor handles requests to the GitHub Copilot API.
 type GitHubCopilotExecutor struct {
-	cfg   *config.Config
-	mu    sync.RWMutex
-	cache map[string]*cachedAPIToken
+	cfg                  *config.Config
+	mu                   sync.RWMutex
+	cache                map[string]*cachedAPIToken
+	warmMu               sync.RWMutex
+	warmCleanupOnce      sync.Once
+	warmPersistMu        sync.Mutex
+	warmStatePath        string
+	warmConversations    map[string]copilotWarmConversationState
+	warmingConversations map[string]time.Time
 }
 
 // cachedAPIToken stores a cached Copilot API token with its expiry.
@@ -61,12 +99,580 @@
 	expiresAt   time.Time
 }
 
+type copilotWarmConversationState struct {
+	LastRealRequestAt time.Time
+	ExpiresAt         time.Time
+}
+
+type copilotWarmConversationDiskState struct {
+	LastRealRequestUnix int64 `json:"last_real_request_unix"`
+	ExpiresUnix         int64 `json:"expires_unix"`
+}
+
 // NewGitHubCopilotExecutor constructs a new executor instance.
 func NewGitHubCopilotExecutor(cfg *config.Config) *GitHubCopilotExecutor {
-	return &GitHubCopilotExecutor{
-		cfg:   cfg,
-		cache: make(map[string]*cachedAPIToken),
+	e := &GitHubCopilotExecutor{
+		cfg:                  cfg,
+		cache:                make(map[string]*cachedAPIToken),
+		warmStatePath:        copilotWarmConversationStatePath(cfg),
+		warmConversations:    make(map[string]copilotWarmConversationState),
+		warmingConversations: make(map[string]time.Time),
+	}
+	e.loadCopilotWarmConversations()
+	return e
+}
+
+func copilotTokenHash(apiToken string) [32]byte {
+	return sha256.Sum256([]byte(apiToken))
+}
+
+func copilotMachineID(apiToken string) string {
+	hash := copilotTokenHash(apiToken)
+	return hex.EncodeToString(hash[:])
+}
+
+func copilotSessionID(apiToken string) string {
+	bucket := time.Now().Unix() / int64(copilotSessionRotation.Seconds())
+	hash := sha256.Sum256([]byte(fmt.Sprintf("%s:%d", apiToken, bucket)))
+	hexStr := hex.EncodeToString(hash[:16])
+	return fmt.Sprintf("%s-%s-%s-%s-%s", hexStr[0:8], hexStr[8:12], hexStr[12:16], hexStr[16:20], hexStr[20:32])
+}
+
+func copilotPickIndex(apiToken, salt string, poolSize int) int {
+	hash := sha256.Sum256([]byte(apiToken + ":" + salt))
+	return int(binary.BigEndian.Uint16(hash[:2]) % uint16(poolSize))
+}
+
+func copilotHashUnitInterval(apiToken, requestID, salt string) float64 {
+	hash := sha256.Sum256([]byte(apiToken + ":" + requestID + ":" + salt))
+	value := binary.BigEndian.Uint64(hash[:8])
+	return float64(value>>11) / (1 << 53)
+}
+
+func copilotWarmConversationStateIsWarm(state copilotWarmConversationState, now time.Time) bool {
+	return !state.ExpiresAt.IsZero() && state.ExpiresAt.After(now)
+}
+
+func copilotWarmConversationStateEqual(a, b copilotWarmConversationState) bool {
+	return a.LastRealRequestAt.Equal(b.LastRealRequestAt) && a.ExpiresAt.Equal(b.ExpiresAt)
+}
+
+func copilotWarmConversationRealInitiatorRatio(lastRealRequestAt, now time.Time) float64 {
+	if lastRealRequestAt.IsZero() || !now.After(lastRealRequestAt) || copilotWarmConversationRealStepWindow <= 0 {
+		return 0
+	}
+
+	steps := int(now.Sub(lastRealRequestAt) / copilotWarmConversationRealStepWindow)
+	if steps <= 0 {
+		return 0
+	}
+
+	maxSteps := int((copilotWarmConversationTTL - time.Nanosecond) / copilotWarmConversationRealStepWindow)
+	if maxSteps < 1 {
+		maxSteps = 1
+	}
+	if steps > maxSteps {
+		steps = maxSteps
+	}
+
+	return float64(steps) * copilotWarmConversationRealStep
+}
+
+func copilotShouldUseWarmConversationRealInitiator(apiToken, requestID string, ratio float64) bool {
+	return ratio > 0 && copilotHashUnitInterval(apiToken, requestID, "warm-real-initiator") < ratio
+}
+
+func copilotWarmConversationStatePath(cfg *config.Config) string {
+	if cfg == nil {
+		return ""
+	}
+	authDir := strings.TrimSpace(cfg.AuthDir)
+	if authDir == "" {
+		return ""
+	}
+	return filepath.Join(authDir, copilotWarmConversationStateFilename)
+}
+
+func (e *GitHubCopilotExecutor) loadCopilotWarmConversations() {
+	if e.warmStatePath == "" {
+		return
+	}
+
+	data, err := os.ReadFile(e.warmStatePath)
+	if err != nil {
+		if !errors.Is(err, os.ErrNotExist) {
+			log.Debugf("github-copilot executor: warm conversation cache read error: %v", err)
+		}
+		return
+	}
+
+	now := time.Now()
+	e.warmMu.Lock()
+	defer e.warmMu.Unlock()
+
+	current := make(map[string]copilotWarmConversationDiskState)
+	if err := json.Unmarshal(data, &current); err == nil {
+		for key, diskState := range current {
+			expiresAt := time.Unix(diskState.ExpiresUnix, 0)
+			if !expiresAt.After(now) {
+				continue
+			}
+			lastRealRequestAt := time.Unix(diskState.LastRealRequestUnix, 0)
+			if diskState.LastRealRequestUnix <= 0 || lastRealRequestAt.After(expiresAt) {
+				lastRealRequestAt = expiresAt.Add(-copilotWarmConversationTTL)
+			}
+			e.warmConversations[key] = copilotWarmConversationState{
+				LastRealRequestAt: lastRealRequestAt,
+				ExpiresAt:         expiresAt,
+			}
+		}
+		return
+	}
+
+	legacy := make(map[string]int64)
+	if err := json.Unmarshal(data, &legacy); err != nil {
+		log.Warnf("github-copilot executor: warm conversation cache decode error: %v", err)
+		return
+	}
+	for key, unixExpiry := range legacy {
+		expiresAt := time.Unix(unixExpiry, 0)
+		if expiresAt.After(now) {
+			e.warmConversations[key] = copilotWarmConversationState{
+				LastRealRequestAt: expiresAt.Add(-copilotWarmConversationTTL),
+				ExpiresAt:         expiresAt,
+			}
+		}
+	}
+}
+
+func (e *GitHubCopilotExecutor) snapshotCopilotWarmConversationsLocked(now time.Time) map[string]copilotWarmConversationState {
+	snapshot := make(map[string]copilotWarmConversationState, len(e.warmConversations))
+	for key, state := range e.warmConversations {
+		if copilotWarmConversationStateIsWarm(state, now) {
+			snapshot[key] = state
+		}
+	}
+	return snapshot
+}
+
+func (e *GitHubCopilotExecutor) persistCopilotWarmConversations(snapshot map[string]copilotWarmConversationState) {
+	if e.warmStatePath == "" {
+		return
+	}
+
+	e.warmPersistMu.Lock()
+	defer e.warmPersistMu.Unlock()
+
+	if len(snapshot) == 0 {
+		if err := os.Remove(e.warmStatePath); err != nil && !errors.Is(err, os.ErrNotExist) {
+			log.Debugf("github-copilot executor: warm conversation cache cleanup error: %v", err)
+		}
+		return
+	}
+
+	if err := os.MkdirAll(filepath.Dir(e.warmStatePath), 0o700); err != nil {
+		log.Warnf("github-copilot executor: warm conversation cache mkdir error: %v", err)
+		return
+	}
+
+	payload := make(map[string]copilotWarmConversationDiskState, len(snapshot))
+	for key, state := range snapshot {
+		payload[key] = copilotWarmConversationDiskState{
+			LastRealRequestUnix: state.LastRealRequestAt.Unix(),
+			ExpiresUnix:         state.ExpiresAt.Unix(),
+		}
+	}
+
+	data, err := json.Marshal(payload)
+	if err != nil {
+		log.Warnf("github-copilot executor: warm conversation cache encode error: %v", err)
+		return
+	}
+
+	tmpPath := e.warmStatePath + ".tmp"
+	if err := os.WriteFile(tmpPath, data, 0o600); err != nil {
+		log.Warnf("github-copilot executor: warm conversation cache write error: %v", err)
+		return
+	}
+	if err := os.Rename(tmpPath, e.warmStatePath); err != nil {
+		_ = os.Remove(tmpPath)
+		log.Warnf("github-copilot executor: warm conversation cache rename error: %v", err)
+	}
+}
+
+func (e *GitHubCopilotExecutor) ensureCopilotWarmConversationCleanup() {
+	e.warmCleanupOnce.Do(func() {
+		go func() {
+			ticker := time.NewTicker(copilotWarmConversationCleanupInterval)
+			defer ticker.Stop()
+			for range ticker.C {
+				e.purgeExpiredCopilotWarmConversations()
+			}
+		}()
+	})
+}
+
+func (e *GitHubCopilotExecutor) purgeExpiredCopilotWarmConversations() {
+	now := time.Now()
+	dirty := false
+	var snapshot map[string]copilotWarmConversationState
+
+	e.warmMu.Lock()
+	for key, expiresAt := range e.warmingConversations {
+		if !expiresAt.After(now) {
+			delete(e.warmingConversations, key)
+		}
+	}
+	for key, state := range e.warmConversations {
+		if !copilotWarmConversationStateIsWarm(state, now) {
+			delete(e.warmConversations, key)
+			dirty = true
+		}
+	}
+	if dirty {
+		snapshot = e.snapshotCopilotWarmConversationsLocked(now)
+	}
+	e.warmMu.Unlock()
+
+	if dirty {
+		e.persistCopilotWarmConversations(snapshot)
+	}
+}
+
+func (e *GitHubCopilotExecutor) getCopilotWarmConversationStateAt(key string, now time.Time) (copilotWarmConversationState, bool) {
+	if key == "" {
+		return copilotWarmConversationState{}, false
+	}
+
+	e.ensureCopilotWarmConversationCleanup()
+	e.warmMu.RLock()
+	state, ok := e.warmConversations[key]
+	e.warmMu.RUnlock()
+	if !ok {
+		return copilotWarmConversationState{}, false
+	}
+	if copilotWarmConversationStateIsWarm(state, now) {
+		return state, true
+	}
+
+	var snapshot map[string]copilotWarmConversationState
+	dirty := false
+	e.warmMu.Lock()
+	if latest, ok := e.warmConversations[key]; ok && !copilotWarmConversationStateIsWarm(latest, now) {
+		delete(e.warmConversations, key)
+		snapshot = e.snapshotCopilotWarmConversationsLocked(now)
+		dirty = true
+	}
+	e.warmMu.Unlock()
+	if dirty {
+		e.persistCopilotWarmConversations(snapshot)
+	}
+	return copilotWarmConversationState{}, false
+}
+
+func (e *GitHubCopilotExecutor) getCopilotWarmConversationState(key string) (copilotWarmConversationState, bool) {
+	return e.getCopilotWarmConversationStateAt(key, time.Now())
+}
+
+func (e *GitHubCopilotExecutor) reserveCopilotWarmConversation(keys []string) bool {
+	if len(keys) == 0 {
+		return false
+	}
+
+	e.ensureCopilotWarmConversationCleanup()
+
+	now := time.Now()
+	reserveUntil := now.Add(copilotWarmConversationReservationTTL)
+
+	e.warmMu.Lock()
+	defer e.warmMu.Unlock()
+	if e.warmingConversations == nil {
+		e.warmingConversations = make(map[string]time.Time)
+	}
+
+	for key, expiresAt := range e.warmingConversations {
+		if !expiresAt.After(now) {
+			delete(e.warmingConversations, key)
+		}
+	}
+
+	for _, key := range keys {
+		if state, ok := e.warmConversations[key]; ok && copilotWarmConversationStateIsWarm(state, now) {
+			return false
+		}
+		if expiresAt, ok := e.warmingConversations[key]; ok && expiresAt.After(now) {
+			return false
+		}
+	}
+
+	for _, key := range keys {
+		e.warmingConversations[key] = reserveUntil
+	}
+	return true
+}
+
+func copilotConversationHasPreviousResponseID(body []byte) bool {
+	if len(body) == 0 {
+		return false
+	}
+	return strings.TrimSpace(gjson.GetBytes(body, "previous_response_id").String()) != ""
+}
+
+func isCopilotWarmConversationRootRole(role string) bool {
+	switch strings.TrimSpace(role) {
+	case "user", "system", "developer":
+		return true
+	default:
+		return false
+	}
+}
+
+func copilotWarmConversationRootKey(apiToken string, body []byte) string {
+	if len(body) == 0 {
+		return ""
+	}
+
+	var root strings.Builder
+	appendRootPart := func(label string, part gjson.Result) {
+		if !part.Exists() {
+			return
+		}
+		root.WriteString(label)
+		root.WriteByte(':')
+		root.WriteString(part.Raw)
+		root.WriteByte('\n')
+	}
+
+	if instructions := gjson.GetBytes(body, "instructions"); instructions.Exists() {
+		appendRootPart("instructions", instructions)
+	}
+	if system := gjson.GetBytes(body, "system"); system.Exists() {
+		appendRootPart("system", system)
+	}
+	if messages := gjson.GetBytes(body, "messages"); messages.Exists() && messages.IsArray() {
+		for _, msg := range messages.Array() {
+			role := msg.Get("role").String()
+			if role == "" {
+				continue
+			}
+			if !isCopilotWarmConversationRootRole(role) {
+				break
+			}
+			appendRootPart("message", msg)
+		}
+	}
+	if root.Len() == 0 {
+		if input := gjson.GetBytes(body, "input"); input.Exists() {
+			if input.IsArray() {
+				for _, item := range input.Array() {
+					itemType := item.Get("type").String()
+					role := item.Get("role").String()
+					if itemType == "" && role != "" {
+						itemType = "message"
+					}
+					if itemType != "message" || !isCopilotWarmConversationRootRole(role) {
+						break
+					}
+					appendRootPart("input", item)
+				}
+			} else if input.Type == gjson.String {
+				appendRootPart("input", input)
+			}
+		}
+	}
+	if root.Len() == 0 {
+		return ""
+	}
+
+	sum := sha256.Sum256([]byte(apiToken + "\n" + root.String()))
+	return "copilot-conversation:" + hex.EncodeToString(sum[:])
+}
+
+func copilotWarmConversationSessionKey(apiToken, sessionID string) string {
+	sessionID = strings.TrimSpace(sessionID)
+	if sessionID == "" {
+		return ""
+	}
+	sum := sha256.Sum256([]byte(apiToken + "\n" + sessionID))
+	return "copilot-session:" + hex.EncodeToString(sum[:])
+}
+
+func copilotExecutionSessionID(ctx context.Context, opts cliproxyexecutor.Options) string {
+	if sessionID := metaStringValue(opts.Metadata, cliproxyexecutor.ExecutionSessionMetadataKey); sessionID != "" {
+		return sessionID
+	}
+	if opts.Headers != nil {
+		if sessionID := strings.TrimSpace(opts.Headers.Get(copilotWarmConversationSessionHeader)); sessionID != "" {
+			return sessionID
+		}
+	}
+	if ctx == nil {
+		return ""
+	}
+	ginCtx, ok := ctx.Value("gin").(*gin.Context)
+	if !ok || ginCtx == nil {
+		return ""
+	}
+	return strings.TrimSpace(ginCtx.GetHeader(copilotWarmConversationSessionHeader))
+}
+
+func copilotWarmConversationKeys(ctx context.Context, opts cliproxyexecutor.Options, apiToken string, body []byte) []string {
+	keys := make([]string, 0, 2)
+	seen := make(map[string]struct{}, 2)
+	addKey := func(key string) {
+		if key == "" {
+			return
+		}
+		if _, ok := seen[key]; ok {
+			return
+		}
+		seen[key] = struct{}{}
+		keys = append(keys, key)
 	}
+
+	addKey(copilotWarmConversationRootKey(apiToken, body))
+	addKey(copilotWarmConversationSessionKey(apiToken, copilotExecutionSessionID(ctx, opts)))
+	return keys
+}
+
+func (e *GitHubCopilotExecutor) clearCopilotWarmConversationReservation(ctx context.Context, opts cliproxyexecutor.Options, apiToken string, body []byte) {
+	keys := copilotWarmConversationKeys(ctx, opts, apiToken, body)
+	if len(keys) == 0 {
+		return
+	}
+
+	now := time.Now()
+	e.warmMu.Lock()
+	for key, expiresAt := range e.warmingConversations {
+		if !expiresAt.After(now) {
+			delete(e.warmingConversations, key)
+		}
+	}
+	for _, key := range keys {
+		delete(e.warmingConversations, key)
+	}
+	e.warmMu.Unlock()
+}
+
+func (e *GitHubCopilotExecutor) markCopilotWarmConversation(ctx context.Context, opts cliproxyexecutor.Options, apiToken string, body []byte, initiator string) {
+	keys := copilotWarmConversationKeys(ctx, opts, apiToken, body)
+	if len(keys) == 0 {
+		return
+	}
+
+	e.ensureCopilotWarmConversationCleanup()
+
+	now := time.Now()
+	var snapshot map[string]copilotWarmConversationState
+
+	e.warmMu.Lock()
+	if e.warmConversations == nil {
+		e.warmConversations = make(map[string]copilotWarmConversationState)
+	}
+	if e.warmingConversations == nil {
+		e.warmingConversations = make(map[string]time.Time)
+	}
+
+	var baseState copilotWarmConversationState
+	hasBaseState := false
+	for _, key := range keys {
+		delete(e.warmingConversations, key)
+		if state, ok := e.warmConversations[key]; ok && copilotWarmConversationStateIsWarm(state, now) {
+			if !hasBaseState || state.ExpiresAt.After(baseState.ExpiresAt) {
+				baseState = state
+				hasBaseState = true
+			}
+		}
+	}
+
+	var nextState copilotWarmConversationState
+	switch strings.TrimSpace(initiator) {
+	case "user":
+		nextState = copilotWarmConversationState{
+			LastRealRequestAt: now,
+			ExpiresAt:         now.Add(copilotWarmConversationTTL),
+		}
+	case "agent":
+		if !hasBaseState {
+			e.warmMu.Unlock()
+			return
+		}
+		nextState = baseState
+	default:
+		e.warmMu.Unlock()
+		return
+	}
+
+	changed := false
+	for _, key := range keys {
+		if state, ok := e.warmConversations[key]; ok && copilotWarmConversationStateIsWarm(state, now) && copilotWarmConversationStateEqual(state, nextState) {
+			continue
+		}
+		e.warmConversations[key] = nextState
+		changed = true
+	}
+	if changed {
+		snapshot = e.snapshotCopilotWarmConversationsLocked(now)
+	}
+	e.warmMu.Unlock()
+
+	if changed {
+		e.persistCopilotWarmConversations(snapshot)
+	}
+}
+
+func (e *GitHubCopilotExecutor) shouldUseCopilotAgentInitiator(ctx context.Context, opts cliproxyexecutor.Options, apiToken, requestID string, body []byte) bool {
+	if len(body) == 0 {
+		return true
+	}
+	if isAgentInitiated(body) || copilotConversationHasPreviousResponseID(body) {
+		return true
+	}
+
+	keys := copilotWarmConversationKeys(ctx, opts, apiToken, body)
+	if len(keys) == 0 {
+		return false
+	}
+	now := time.Now()
+	sessionKey := copilotWarmConversationSessionKey(apiToken, copilotExecutionSessionID(ctx, opts))
+	if sessionKey != "" {
+		if _, ok := e.getCopilotWarmConversationStateAt(sessionKey, now); ok {
+			return true
+		}
+	}
+	for _, key := range keys {
+		if key == sessionKey {
+			continue
+		}
+		if state, ok := e.getCopilotWarmConversationStateAt(key, now); ok {
+			ratio := copilotWarmConversationRealInitiatorRatio(state.LastRealRequestAt, now)
+			return !copilotShouldUseWarmConversationRealInitiator(apiToken, requestID, ratio)
+		}
+	}
+	if e.reserveCopilotWarmConversation(keys) {
+		return false
+	}
+	return true
+}
+
+func copilotHeaderFingerprint(apiToken string) (userAgent, editorVersion, pluginVersion, sessionID, machineID string) {
+	userAgent = copilotUserAgent
+	editorVersion = copilotEditorVersion
+	pluginVersion = copilotPluginVersion
+	if strings.TrimSpace(apiToken) == "" {
+		return userAgent, editorVersion, pluginVersion, uuid.NewString(), uuid.NewString()
+	}
+	userAgent = copilotUserAgents[copilotPickIndex(apiToken, "ua", len(copilotUserAgents))]
+	editorVersion = copilotEditorVersions[copilotPickIndex(apiToken, "editor", len(copilotEditorVersions))]
+	pluginVersion = copilotPluginVersions[copilotPickIndex(apiToken, "plugin", len(copilotPluginVersions))]
+	return userAgent, editorVersion, pluginVersion, copilotSessionID(apiToken), copilotMachineID(apiToken)
+}
+
+func copilotAPIKeyFromAuth(auth *cliproxyauth.Auth) string {
+	if auth == nil || auth.Attributes == nil {
+		return ""
+	}
+	return strings.TrimSpace(auth.Attributes["api_key"])
 }
 
 // Identifier implements ProviderExecutor.
@@ -85,7 +691,7 @@
 	if errToken != nil {
 		return errToken
 	}
-	e.applyHeaders(req, apiToken, nil)
+	e.applyHeaders(req, apiToken, nil, ctx, cliproxyexecutor.Options{})
 	return nil
 }
 
@@ -107,10 +713,16 @@
 
 // Execute handles non-streaming requests to GitHub Copilot.
 func (e *GitHubCopilotExecutor) Execute(ctx context.Context, auth *cliproxyauth.Auth, req cliproxyexecutor.Request, opts cliproxyexecutor.Options) (resp cliproxyexecutor.Response, err error) {
-	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req); errGateway != nil {
+	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req, opts); errGateway != nil {
 		return resp, errGateway
 	} else if ok {
-		return nativeExec.Execute(ctx, nativeAuth, nativeReq, opts)
+		resp, err = nativeExec.Execute(ctx, nativeAuth, nativeReq, opts)
+		if err != nil {
+			e.clearCopilotWarmConversationReservation(ctx, opts, copilotAPIKeyFromAuth(nativeAuth), req.Payload)
+			return resp, err
+		}
+		e.markCopilotWarmConversation(ctx, opts, copilotAPIKeyFromAuth(nativeAuth), req.Payload, nativeAuth.Attributes["header:X-Initiator"])
+		return resp, nil
 	}
 
 	apiToken, baseURL, errToken := e.ensureAPIToken(ctx, auth)
@@ -169,7 +781,7 @@
 	if err != nil {
 		return resp, err
 	}
-	e.applyHeaders(httpReq, apiToken, body)
+	e.applyHeaders(httpReq, apiToken, body, ctx, opts)
 
 	// Add Copilot-Vision-Request header if the request contains vision content
 	if hasVision {
@@ -197,6 +809,7 @@
 	httpClient := newProxyAwareHTTPClient(ctx, e.cfg, auth, 0)
 	httpResp, err := httpClient.Do(httpReq)
 	if err != nil {
+		e.clearCopilotWarmConversationReservation(ctx, opts, apiToken, body)
 		recordAPIResponseError(ctx, e.cfg, err)
 		return resp, err
 	}
@@ -209,6 +822,7 @@
 	recordAPIResponseMetadata(ctx, e.cfg, httpResp.StatusCode, httpResp.Header.Clone())
 
 	if !isHTTPSuccess(httpResp.StatusCode) {
+		e.clearCopilotWarmConversationReservation(ctx, opts, apiToken, body)
 		data, _ := io.ReadAll(httpResp.Body)
 		appendAPIResponseChunk(ctx, e.cfg, data)
 		log.Debugf("github-copilot executor: upstream error status: %d, body: %s", httpResp.StatusCode, summarizeErrorBody(httpResp.Header.Get("Content-Type"), data))
@@ -218,10 +832,12 @@
 
 	data, err := io.ReadAll(httpResp.Body)
 	if err != nil {
+		e.clearCopilotWarmConversationReservation(ctx, opts, apiToken, body)
 		recordAPIResponseError(ctx, e.cfg, err)
 		return resp, err
 	}
 	appendAPIResponseChunk(ctx, e.cfg, data)
+	e.markCopilotWarmConversation(ctx, opts, apiToken, body, strings.TrimSpace(httpReq.Header.Get("X-Initiator")))
 
 	detail := parseOpenAIUsage(data)
 	if useResponses && detail.TotalTokens == 0 {
@@ -246,10 +862,16 @@
 
 // ExecuteStream handles streaming requests to GitHub Copilot.
 func (e *GitHubCopilotExecutor) ExecuteStream(ctx context.Context, auth *cliproxyauth.Auth, req cliproxyexecutor.Request, opts cliproxyexecutor.Options) (_ *cliproxyexecutor.StreamResult, err error) {
-	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req); errGateway != nil {
+	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req, opts); errGateway != nil {
 		return nil, errGateway
 	} else if ok {
-		return nativeExec.ExecuteStream(ctx, nativeAuth, nativeReq, opts)
+		result, execErr := nativeExec.ExecuteStream(ctx, nativeAuth, nativeReq, opts)
+		if execErr != nil {
+			e.clearCopilotWarmConversationReservation(ctx, opts, copilotAPIKeyFromAuth(nativeAuth), req.Payload)
+			return nil, execErr
+		}
+		e.markCopilotWarmConversation(ctx, opts, copilotAPIKeyFromAuth(nativeAuth), req.Payload, nativeAuth.Attributes["header:X-Initiator"])
+		return result, nil
 	}
 
 	apiToken, baseURL, errToken := e.ensureAPIToken(ctx, auth)
@@ -312,7 +934,7 @@
 	if err != nil {
 		return nil, err
 	}
-	e.applyHeaders(httpReq, apiToken, body)
+	e.applyHeaders(httpReq, apiToken, body, ctx, opts)
 
 	// Add Copilot-Vision-Request header if the request contains vision content
 	if hasVision {
@@ -340,6 +962,7 @@
 	httpClient := newProxyAwareHTTPClient(ctx, e.cfg, auth, 0)
 	httpResp, err := httpClient.Do(httpReq)
 	if err != nil {
+		e.clearCopilotWarmConversationReservation(ctx, opts, apiToken, body)
 		recordAPIResponseError(ctx, e.cfg, err)
 		return nil, err
 	}
@@ -347,6 +970,7 @@
 	recordAPIResponseMetadata(ctx, e.cfg, httpResp.StatusCode, httpResp.Header.Clone())
 
 	if !isHTTPSuccess(httpResp.StatusCode) {
+		e.clearCopilotWarmConversationReservation(ctx, opts, apiToken, body)
 		data, readErr := io.ReadAll(httpResp.Body)
 		if errClose := httpResp.Body.Close(); errClose != nil {
 			log.Errorf("github-copilot executor: close response body error: %v", errClose)
@@ -361,6 +985,8 @@
 		return nil, err
 	}
 
+	e.markCopilotWarmConversation(ctx, opts, apiToken, body, strings.TrimSpace(httpReq.Header.Get("X-Initiator")))
+
 	out := make(chan cliproxyexecutor.StreamChunk)
 
 	go func() {
@@ -436,7 +1062,7 @@
 // CountTokens estimates token count locally using tiktoken, since the GitHub
 // Copilot API does not expose a dedicated token counting endpoint.
 func (e *GitHubCopilotExecutor) CountTokens(ctx context.Context, auth *cliproxyauth.Auth, req cliproxyexecutor.Request, opts cliproxyexecutor.Options) (cliproxyexecutor.Response, error) {
-	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req); errGateway != nil {
+	if nativeExec, nativeAuth, nativeReq, ok, errGateway := e.nativeGateway(ctx, auth, req, opts); errGateway != nil {
 		return cliproxyexecutor.Response{}, errGateway
 	} else if ok {
 		return nativeExec.CountTokens(ctx, nativeAuth, nativeReq, opts)
@@ -490,6 +1116,7 @@
 	ctx context.Context,
 	auth *cliproxyauth.Auth,
 	req cliproxyexecutor.Request,
+	opts cliproxyexecutor.Options,
 ) (cliproxyauth.ProviderExecutor, *cliproxyauth.Auth, cliproxyexecutor.Request, bool, error) {
 	if !githubCopilotUsesAnthropicGateway(req.Model) {
 		return nil, nil, req, false, nil
@@ -501,7 +1128,7 @@
 	if err != nil {
 		return nil, nil, req, false, err
 	}
-	nativeAuth := buildCopilotAnthropicGatewayAuth(auth, apiToken, baseURL, req.Payload)
+	nativeAuth := e.buildCopilotAnthropicGatewayAuth(ctx, opts, auth, apiToken, baseURL, req.Payload)
 	if nativeAuth == nil {
 		return nil, nil, req, false, nil
 	}
@@ -514,7 +1141,7 @@
 	return strings.HasPrefix(baseModel, "claude-")
 }
 
-func buildCopilotAnthropicGatewayAuth(auth *cliproxyauth.Auth, apiToken, baseURL string, body []byte) *cliproxyauth.Auth {
+func (e *GitHubCopilotExecutor) buildCopilotAnthropicGatewayAuth(ctx context.Context, opts cliproxyexecutor.Options, auth *cliproxyauth.Auth, apiToken, baseURL string, body []byte) *cliproxyauth.Auth {
 	apiToken = strings.TrimSpace(apiToken)
 	baseURL = strings.TrimRight(strings.TrimSpace(baseURL), "/")
 	if apiToken == "" || baseURL == "" {
@@ -529,22 +1156,26 @@
 	if nativeAuth.Attributes == nil {
 		nativeAuth.Attributes = make(map[string]string)
 	}
+	userAgent, editorVersion, pluginVersion, sessionID, machineID := copilotHeaderFingerprint(apiToken)
+	requestID := uuid.NewString()
+	initiator := "user"
+	if e.shouldUseCopilotAgentInitiator(ctx, opts, apiToken, requestID, body) {
+		initiator = "agent"
+	}
 	nativeAuth.Attributes["api_key"] = apiToken
 	nativeAuth.Attributes["base_url"] = baseURL
 	nativeAuth.Attributes["header:Content-Type"] = "application/json"
 	nativeAuth.Attributes["header:Accept"] = "application/json"
-	nativeAuth.Attributes["header:User-Agent"] = copilotUserAgent
-	nativeAuth.Attributes["header:Editor-Version"] = copilotEditorVersion
-	nativeAuth.Attributes["header:Editor-Plugin-Version"] = copilotPluginVersion
+	nativeAuth.Attributes["header:User-Agent"] = userAgent
+	nativeAuth.Attributes["header:Editor-Version"] = editorVersion
+	nativeAuth.Attributes["header:Editor-Plugin-Version"] = pluginVersion
 	nativeAuth.Attributes["header:Openai-Intent"] = copilotOpenAIIntent
 	nativeAuth.Attributes["header:Copilot-Integration-Id"] = copilotIntegrationID
 	nativeAuth.Attributes["header:X-Github-Api-Version"] = copilotGitHubAPIVer
-	nativeAuth.Attributes["header:X-Request-Id"] = uuid.NewString()
-	if isAgentInitiated(body) {
-		nativeAuth.Attributes["header:X-Initiator"] = "agent"
-	} else {
-		nativeAuth.Attributes["header:X-Initiator"] = "user"
-	}
+	nativeAuth.Attributes["header:X-Request-Id"] = requestID
+	nativeAuth.Attributes["header:X-Initiator"] = initiator
+	nativeAuth.Attributes["header:VScode-SessionId"] = sessionID
+	nativeAuth.Attributes["header:VScode-MachineId"] = machineID
 	if detectVisionContent(body) {
 		nativeAuth.Attributes["header:Copilot-Vision-Request"] = "true"
 	}
@@ -601,20 +1232,29 @@
 }
 
 // applyHeaders sets the required headers for GitHub Copilot API requests.
-func (e *GitHubCopilotExecutor) applyHeaders(r *http.Request, apiToken string, body []byte) {
+func (e *GitHubCopilotExecutor) applyHeaders(r *http.Request, apiToken string, body []byte, ctx context.Context, opts cliproxyexecutor.Options) {
 	r.Header.Set("Content-Type", "application/json")
 	r.Header.Set("Authorization", "Bearer "+apiToken)
 	r.Header.Set("Accept", "application/json")
-	r.Header.Set("User-Agent", copilotUserAgent)
-	r.Header.Set("Editor-Version", copilotEditorVersion)
-	r.Header.Set("Editor-Plugin-Version", copilotPluginVersion)
+	userAgent, editorVersion, pluginVersion, sessionID, machineID := copilotHeaderFingerprint(apiToken)
+	r.Header.Set("User-Agent", userAgent)
+	r.Header.Set("Editor-Version", editorVersion)
+	r.Header.Set("Editor-Plugin-Version", pluginVersion)
 	r.Header.Set("Openai-Intent", copilotOpenAIIntent)
 	r.Header.Set("Copilot-Integration-Id", copilotIntegrationID)
 	r.Header.Set("X-Github-Api-Version", copilotGitHubAPIVer)
-	r.Header.Set("X-Request-Id", uuid.NewString())
-	r.Header.Set("X-Initiator", "agent")
-	r.Header.Set("VScode-SessionId", uuid.NewString())
-	r.Header.Set("VScode-MachineId", uuid.NewString())
+	requestID := strings.TrimSpace(r.Header.Get("X-Request-Id"))
+	if requestID == "" {
+		requestID = uuid.NewString()
+	}
+	r.Header.Set("X-Request-Id", requestID)
+	initiator := "user"
+	if e.shouldUseCopilotAgentInitiator(ctx, opts, apiToken, requestID, body) {
+		initiator = "agent"
+	}
+	r.Header.Set("X-Initiator", initiator)
+	r.Header.Set("VScode-SessionId", sessionID)
+	r.Header.Set("VScode-MachineId", machineID)
 }
 
 // isAgentInitiated determines whether the current request is agent-initiated
diff --git a/internal/runtime/executor/github_copilot_executor_test.go b/internal/runtime/executor/github_copilot_executor_test.go
--- a/internal/runtime/executor/github_copilot_executor_test.go
+++ b/internal/runtime/executor/github_copilot_executor_test.go
@@ -2,6 +2,7 @@
 
 import (
 	"context"
+	"fmt"
 	"io"
 	"net/http"
 	"net/http/httptest"
@@ -330,12 +331,30 @@
 
 // --- Tests for X-Initiator detection logic (Problem L) ---
 
+func findCopilotWarmConversationRequestID(t *testing.T, ratio float64, wantReal bool) string {
+	t.Helper()
+
+	if !wantReal && ratio <= 0 {
+		return "req-0"
+	}
+
+	for i := 0; i < 100000; i++ {
+		requestID := fmt.Sprintf("req-%d", i)
+		if copilotShouldUseWarmConversationRealInitiator("token", requestID, ratio) == wantReal {
+			return requestID
+		}
+	}
+
+	t.Fatalf("could not find request ID with ratio=%v wantReal=%v", ratio, wantReal)
+	return ""
+}
+
 func TestApplyHeaders_XInitiator_UserOnly(t *testing.T) {
 	t.Parallel()
 	e := &GitHubCopilotExecutor{}
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
 	body := []byte(`{"messages":[{"role":"system","content":"sys"},{"role":"user","content":"hello"}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "user" {
 		t.Fatalf("X-Initiator = %q, want user", got)
 	}
@@ -349,7 +368,7 @@
 	// the request is a continuation (e.g. Claude tool result translated to a
 	// synthetic user message). Should be "agent".
 	body := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":"I will read the file"},{"role":"user","content":[{"type":"tool_result","tool_use_id":"tu1","content":"file contents..."}]}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "agent" {
 		t.Fatalf("X-Initiator = %q, want agent (last user contains tool_result)", got)
 	}
@@ -361,7 +380,7 @@
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
 	// When the last message has role "tool", it's clearly agent-initiated.
 	body := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"tool","content":"result"}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "agent" {
 		t.Fatalf("X-Initiator = %q, want agent (last role is tool)", got)
 	}
@@ -372,7 +391,7 @@
 	e := &GitHubCopilotExecutor{}
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
 	body := []byte(`{"input":[{"type":"message","role":"user","content":[{"type":"input_text","text":"Hi"}]},{"type":"message","role":"assistant","content":[{"type":"output_text","text":"Hello"}]}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "agent" {
 		t.Fatalf("X-Initiator = %q, want agent (last role is assistant)", got)
 	}
@@ -384,7 +403,7 @@
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
 	// Responses API: last item is user-role but history contains assistant → agent.
 	body := []byte(`{"input":[{"type":"message","role":"assistant","content":[{"type":"output_text","text":"I can help"}]},{"type":"message","role":"user","content":[{"type":"input_text","text":"Do X"}]}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "agent" {
 		t.Fatalf("X-Initiator = %q, want agent (history has assistant)", got)
 	}
@@ -395,7 +414,7 @@
 	e := &GitHubCopilotExecutor{}
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
 	body := []byte(`{"input":[{"type":"message","role":"user","content":[{"type":"input_text","text":"Use tool"}]},{"type":"function_call","call_id":"c1","name":"Read","arguments":"{}"},{"type":"function_call_output","call_id":"c1","output":"ok"}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "agent" {
 		t.Fatalf("X-Initiator = %q, want agent (last item maps to tool role)", got)
 	}
@@ -408,7 +427,7 @@
 	// Genuine multi-turn: user → assistant (plain text) → user follow-up.
 	// No tool messages → should be "user" (not a false-positive).
 	body := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":"Hi there!"},{"role":"user","content":"what is 2+2?"}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "user" {
 		t.Fatalf("X-Initiator = %q, want user (genuine multi-turn, no tools)", got)
 	}
@@ -422,21 +441,229 @@
 	// The last message is a genuine user question — should be "user", not "agent".
 	// This aligns with opencode's behavior: only active tool loops are agent-initiated.
 	body := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":[{"type":"tool_use","id":"tu1","name":"Read","input":{}}]},{"role":"tool","tool_call_id":"tu1","content":"file data"},{"role":"assistant","content":"I read the file."},{"role":"user","content":"What did we do so far?"}]}`)
-	e.applyHeaders(req, "token", body)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("X-Initiator"); got != "user" {
 		t.Fatalf("X-Initiator = %q, want user (genuine follow-up after tool history)", got)
 	}
 }
 
+func TestCopilotWarmConversationRealInitiatorRatio(t *testing.T) {
+	t.Parallel()
+
+	now := time.Unix(1_762_000_000, 0)
+	tests := []struct {
+		name     string
+		lastReal time.Time
+		want     float64
+	}{
+		{name: "zero before first step", lastReal: now.Add(-90 * time.Minute), want: 0},
+		{name: "first step after two hours", lastReal: now.Add(-2 * time.Hour), want: 0.005},
+		{name: "second step after four hours", lastReal: now.Add(-4 * time.Hour), want: 0.01},
+		{name: "clamped before expiry", lastReal: now.Add(-(copilotWarmConversationTTL - time.Minute)), want: 0.01},
+	}
+
+	for _, tt := range tests {
+		tt := tt
+		t.Run(tt.name, func(t *testing.T) {
+			t.Parallel()
+			if got := copilotWarmConversationRealInitiatorRatio(tt.lastReal, now); got != tt.want {
+				t.Fatalf("ratio = %v, want %v", got, tt.want)
+			}
+		})
+	}
+}
+
+func TestApplyHeaders_XInitiator_WarmConversationUsesAgent(t *testing.T) {
+	t.Parallel()
+
+	e := &GitHubCopilotExecutor{
+		warmConversations: make(map[string]copilotWarmConversationState),
+	}
+	rootBody := []byte(`{"messages":[{"role":"user","content":"hello"}]}`)
+	followUpBody := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":"Hi there!"},{"role":"user","content":"what is 2+2?"}]}`)
+	e.markCopilotWarmConversation(nil, cliproxyexecutor.Options{}, "token", rootBody, "user")
+
+	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	req.Header.Set("X-Request-Id", findCopilotWarmConversationRequestID(t, 0, false))
+	e.applyHeaders(req, "token", followUpBody, nil, cliproxyexecutor.Options{})
+	if got := req.Header.Get("X-Initiator"); got != "agent" {
+		t.Fatalf("X-Initiator = %q, want agent for warmed conversation", got)
+	}
+}
+
+func TestApplyHeaders_XInitiator_WarmConversationUsesRampedUserNoise(t *testing.T) {
+	t.Parallel()
+
+	now := time.Now()
+	followUpBody := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":"Hi there!"},{"role":"user","content":"what is 2+2?"}]}`)
+	rootKey := copilotWarmConversationRootKey("token", followUpBody)
+	e := &GitHubCopilotExecutor{
+		warmConversations: map[string]copilotWarmConversationState{
+			rootKey: {
+				LastRealRequestAt: now.Add(-4 * time.Hour),
+				ExpiresAt:         now.Add(2 * time.Hour),
+			},
+		},
+	}
+
+	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	req.Header.Set("X-Request-Id", findCopilotWarmConversationRequestID(t, 0.01, true))
+	e.applyHeaders(req, "token", followUpBody, nil, cliproxyexecutor.Options{})
+	if got := req.Header.Get("X-Initiator"); got != "user" {
+		t.Fatalf("X-Initiator = %q, want user for ramped warm-conversation noise", got)
+	}
+}
+
+func TestApplyHeaders_XInitiator_WarmExecutionSessionAlwaysUsesAgent(t *testing.T) {
+	t.Parallel()
+
+	e := &GitHubCopilotExecutor{
+		warmConversations: make(map[string]copilotWarmConversationState),
+	}
+	sessionID := "ses_test_warm_session"
+	opts := cliproxyexecutor.Options{
+		Headers: http.Header{copilotWarmConversationSessionHeader: []string{sessionID}},
+	}
+	rootBody := []byte(`{"messages":[{"role":"user","content":"hello"}]}`)
+	branchedRootBody := []byte(`{"messages":[{"role":"user","content":"branch from the earlier point"}]}`)
+	e.markCopilotWarmConversation(nil, opts, "token", rootBody, "user")
+
+	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	req.Header.Set("X-Request-Id", findCopilotWarmConversationRequestID(t, 0.01, true))
+	e.applyHeaders(req, "token", branchedRootBody, nil, opts)
+	if got := req.Header.Get("X-Initiator"); got != "agent" {
+		t.Fatalf("X-Initiator = %q, want agent for active warm execution session", got)
+	}
+}
+
+func TestMarkCopilotWarmConversation_AgentPreservesLastRealAndCopiesSessionKey(t *testing.T) {
+	t.Parallel()
+
+	now := time.Now()
+	rootBody := []byte(`{"messages":[{"role":"user","content":"hello"}]}`)
+	sessionID := "ses_test_copy"
+	opts := cliproxyexecutor.Options{
+		Headers: http.Header{copilotWarmConversationSessionHeader: []string{sessionID}},
+	}
+	rootKey := copilotWarmConversationRootKey("token", rootBody)
+	sessionKey := copilotWarmConversationSessionKey("token", sessionID)
+	existing := copilotWarmConversationState{
+		LastRealRequestAt: now.Add(-4 * time.Hour),
+		ExpiresAt:         now.Add(2 * time.Hour),
+	}
+
+	e := &GitHubCopilotExecutor{
+		warmConversations: map[string]copilotWarmConversationState{
+			rootKey: existing,
+		},
+	}
+
+	e.markCopilotWarmConversation(nil, opts, "token", rootBody, "agent")
+
+	gotRoot, ok := e.getCopilotWarmConversationState(rootKey)
+	if !ok {
+		t.Fatal("expected root key to remain warm")
+	}
+	if !copilotWarmConversationStateEqual(gotRoot, existing) {
+		t.Fatalf("root state changed unexpectedly: got %+v want %+v", gotRoot, existing)
+	}
+	gotSession, ok := e.getCopilotWarmConversationState(sessionKey)
+	if !ok {
+		t.Fatal("expected session key to inherit warm state")
+	}
+	if !copilotWarmConversationStateEqual(gotSession, existing) {
+		t.Fatalf("session state = %+v, want %+v", gotSession, existing)
+	}
+}
+
+func TestMarkCopilotWarmConversation_UserResetsSessionWindow(t *testing.T) {
+	t.Parallel()
+
+	rootBody := []byte(`{"messages":[{"role":"user","content":"hello"}]}`)
+	rootKey := copilotWarmConversationRootKey("token", rootBody)
+	e := &GitHubCopilotExecutor{
+		warmConversations: map[string]copilotWarmConversationState{
+			rootKey: {
+				LastRealRequestAt: time.Now().Add(-4 * time.Hour),
+				ExpiresAt:         time.Now().Add(2 * time.Hour),
+			},
+		},
+	}
+
+	before := time.Now()
+	e.markCopilotWarmConversation(nil, cliproxyexecutor.Options{}, "token", rootBody, "user")
+	after := time.Now()
+
+	state, ok := e.getCopilotWarmConversationState(rootKey)
+	if !ok {
+		t.Fatal("expected root key to remain warm")
+	}
+	if state.LastRealRequestAt.Before(before) || state.LastRealRequestAt.After(after) {
+		t.Fatalf("last real request timestamp = %v, want between %v and %v", state.LastRealRequestAt, before, after)
+	}
+	if state.ExpiresAt.Before(before.Add(copilotWarmConversationTTL)) || state.ExpiresAt.After(after.Add(copilotWarmConversationTTL)) {
+		t.Fatalf("expires_at = %v, want reset to a fresh %v window", state.ExpiresAt, copilotWarmConversationTTL)
+	}
+}
+
+func TestApplyHeaders_XInitiator_WarmConversationPersistsAcrossExecutorRestart(t *testing.T) {
+	t.Parallel()
+
+	cfg := &config.Config{AuthDir: t.TempDir()}
+	rootBody := []byte(`{"messages":[{"role":"user","content":"hello"}]}`)
+	followUpBody := []byte(`{"messages":[{"role":"user","content":"hello"},{"role":"assistant","content":"Hi there!"},{"role":"user","content":"what is 2+2?"}]}`)
+
+	e1 := NewGitHubCopilotExecutor(cfg)
+	e1.markCopilotWarmConversation(nil, cliproxyexecutor.Options{}, "token", rootBody, "user")
+
+	e2 := NewGitHubCopilotExecutor(cfg)
+	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	req.Header.Set("X-Request-Id", findCopilotWarmConversationRequestID(t, 0, false))
+	e2.applyHeaders(req, "token", followUpBody, nil, cliproxyexecutor.Options{})
+	if got := req.Header.Get("X-Initiator"); got != "agent" {
+		t.Fatalf("X-Initiator = %q, want agent for warmed conversation restored from disk", got)
+	}
+}
+
+func TestApplyHeaders_XInitiator_PreviousResponseIDAlwaysAgent(t *testing.T) {
+	t.Parallel()
+
+	e := &GitHubCopilotExecutor{}
+	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	body := []byte(`{"previous_response_id":"resp_123","input":[{"type":"message","role":"user","content":[{"type":"input_text","text":"follow up"}]}]}`)
+	e.applyHeaders(req, "token", body, nil, cliproxyexecutor.Options{})
+	if got := req.Header.Get("X-Initiator"); got != "agent" {
+		t.Fatalf("X-Initiator = %q, want agent when previous_response_id is present", got)
+	}
+}
+
+func TestApplyHeaders_VScodeIDsStablePerToken(t *testing.T) {
+	t.Parallel()
+
+	e := &GitHubCopilotExecutor{}
+	req1, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+	req2, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
+
+	e.applyHeaders(req1, "token", nil, nil, cliproxyexecutor.Options{})
+	e.applyHeaders(req2, "token", nil, nil, cliproxyexecutor.Options{})
+
+	if got1, got2 := req1.Header.Get("VScode-MachineId"), req2.Header.Get("VScode-MachineId"); got1 != got2 {
+		t.Fatalf("VScode-MachineId should be stable per token, got %q and %q", got1, got2)
+	}
+	if got1, got2 := req1.Header.Get("VScode-SessionId"), req2.Header.Get("VScode-SessionId"); got1 != got2 {
+		t.Fatalf("VScode-SessionId should be stable within a rotation bucket, got %q and %q", got1, got2)
+	}
+}
+
 // --- Tests for x-github-api-version header (Problem M) ---
 
 func TestApplyHeaders_GitHubAPIVersion(t *testing.T) {
 	t.Parallel()
 	e := &GitHubCopilotExecutor{}
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
-	e.applyHeaders(req, "token", nil)
-	if got := req.Header.Get("X-Github-Api-Version"); got != "2025-04-01" {
-		t.Fatalf("X-Github-Api-Version = %q, want 2025-04-01", got)
+	e.applyHeaders(req, "token", nil, nil, cliproxyexecutor.Options{})
+	if got := req.Header.Get("X-Github-Api-Version"); got != copilotGitHubAPIVer {
+		t.Fatalf("X-Github-Api-Version = %q, want %q", got, copilotGitHubAPIVer)
 	}
 }
 
@@ -599,7 +826,7 @@
 	t.Parallel()
 	e := &GitHubCopilotExecutor{}
 	req, _ := http.NewRequest(http.MethodPost, "https://example.com", nil)
-	e.applyHeaders(req, "token", nil)
+	e.applyHeaders(req, "token", nil, nil, cliproxyexecutor.Options{})
 	if got := req.Header.Get("Openai-Intent"); got != "conversation-edits" {
 		t.Fatalf("Openai-Intent = %q, want conversation-edits", got)
 	}
@@ -664,6 +891,8 @@
 	var gotEditorVersion string
 	var gotIntent string
 	var gotInitiator string
+	var gotSessionID string
+	var gotMachineID string
 	var gotBody []byte
 
 	server := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
@@ -674,6 +903,8 @@
 		gotEditorVersion = r.Header.Get("Editor-Version")
 		gotIntent = r.Header.Get("Openai-Intent")
 		gotInitiator = r.Header.Get("X-Initiator")
+		gotSessionID = r.Header.Get("VScode-SessionId")
+		gotMachineID = r.Header.Get("VScode-MachineId")
 		gotBody, _ = io.ReadAll(r.Body)
 		w.Header().Set("Content-Type", "application/json")
 		_, _ = w.Write([]byte(`{"id":"msg_1","type":"message","model":"claude-sonnet-4.6","role":"assistant","content":[{"type":"text","text":"ok"}],"usage":{"input_tokens":1,"output_tokens":1}}`))
@@ -712,8 +943,9 @@
 	if gotAPIVersion != copilotGitHubAPIVer {
 		t.Fatalf("X-Github-Api-Version = %q, want %q", gotAPIVersion, copilotGitHubAPIVer)
 	}
-	if gotEditorVersion != copilotEditorVersion {
-		t.Fatalf("Editor-Version = %q, want %q", gotEditorVersion, copilotEditorVersion)
+	_, expectedEditorVersion, _, _, _ := copilotHeaderFingerprint("copilot-api-token")
+	if gotEditorVersion != expectedEditorVersion {
+		t.Fatalf("Editor-Version = %q, want %q", gotEditorVersion, expectedEditorVersion)
 	}
 	if gotIntent != copilotOpenAIIntent {
 		t.Fatalf("Openai-Intent = %q, want %q", gotIntent, copilotOpenAIIntent)
@@ -727,6 +959,9 @@
 	if gjson.GetBytes(resp.Payload, "content.0.text").String() != "ok" {
 		t.Fatalf("response text = %q, want %q", gjson.GetBytes(resp.Payload, "content.0.text").String(), "ok")
 	}
+	if gotSessionID == "" || gotMachineID == "" {
+		t.Fatalf("expected VScode session headers to be set, got session=%q machine=%q", gotSessionID, gotMachineID)
+	}
 }
 
 func TestGitHubCopilotExecute_CopilotPrefixedClaudeModelUsesNativeGateway(t *testing.T) {
